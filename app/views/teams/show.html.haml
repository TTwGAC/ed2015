%p
  Name:
  = @team.name
%p
  Slogan:
  = @team.slogan
%p
  Description:
  = @team.description
%p
  Members:
  - @team.users.each do |user|
    %a{:href => "/users/#{user.id}"}
      = user.first_name
      = user.last_name
    %br

%p#notice= notice
- if can? :edit, @team
  = link_to 'Edit', edit_team_path(@team)
  |
= link_to 'Back', teams_path
