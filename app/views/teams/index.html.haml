%h1 Listing teams
%table.table
  %tr
    %th Name
    %th # Players
    %th Slogan
    - if can? :manage, Team
      %th Paid?
  - @teams.each do |team|
    %tr
      %td= link_to team.name, team
      %td= team.players.count
      %td= team.slogan
      - if can? :manage, Team
        %td= team.paid ? "Yes" : "No"
%br/
- if can? :create, Team
  = link_to 'New Team', new_team_path
