%h1 Listing clusters
%table
  %tr
    %th Name
    %th Sequence
    %th Color
    %th
    %th
  - @clusters.each do |cluster|
    %tr
      %td= cluster.name
      %td= cluster.sequence
      %td= cluster.color.capitalize
      %td= link_to 'Show', cluster
      %td= link_to 'Edit', edit_cluster_path(cluster)
      %td= link_to 'Destroy', cluster, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Cluster', new_cluster_path
