%p
  Name:
  = @user.name
%p
  Slogan:
  = @user.slogan
%p
  Description:
  = @user.description
%p
  Members:
  - @user.users.each do |user|
    %a{:href => "/user/#{user.id}"}
      = user.first_name
      = user.last_name
    %br

%p#notice= notice
= link_to 'Edit', edit_user_path(@user)
|
\#{link_to 'Back', users_path}
