%h1 Listing users
%table
  %tr
    %th Name
    %th Email
    %th Actions
  - @users.each do |user|
    %tr
      %td
        = user.first_name
        = "\"#{user.nickname}\""
        = user.last_name
      %td= user.email
      %td
        = link_to 'Show', user
        = link_to 'Edit', edit_user_path(user)
        = link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }
%br/
